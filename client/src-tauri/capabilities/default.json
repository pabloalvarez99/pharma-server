{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Pharma Client window on EVERY platform (desktop + Android/iOS). Custom `invoke` commands (login, license_status, server_health, logout) do not require a permission entry; only plugin APIs do. Desktop-only plugin permissions live in `desktop.json`.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}