{
  "name": "rutbusiness-client",
  "private": true,
  "version": "0.1.0",
  "description": "RutBusiness Client — Tauri 2 desktop launcher for pharma-server (LoL-style login -> ERP shell)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:web": "vite --mode web",
    "build": "tsc --noEmit && vite build",
    "build:web": "tsc --noEmit && vite build --mode web",
    "test": "vitest run",
    "e2e": "node e2e/run.mjs",
    "gate": "npm run build && npm run test && npm run build:web && npm run e2e",
    "preview": "vite preview",
    "tauri": "tauri",
    "android:init": "tauri android init",
    "android:dev": "tauri android dev",
    "android:build": "tauri android build --apk",
    "ios:init": "tauri ios init",
    "ios:dev": "tauri ios dev"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "@tauri-apps/plugin-updater": "^2.10.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "happy-dom": "^20.10.4",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.6"
  }
}
