// Web stub for `@tauri-apps/plugin-updater` (aliased in `--mode web`). The
// browser build updates itself on every deploy — there is nothing to check.
// `src/updater.ts` treats a null result as "no update", so login is untouched.

export async function check(): Promise<null> {
  return null;
}
