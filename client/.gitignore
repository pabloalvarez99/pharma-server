node_modules/
dist/
.vite/
# package-lock.json IS committed — `npm ci` (build-client.ps1 + CI) needs it for
# reproducible installs. Do not re-ignore it.
src-tauri/target/
src-tauri/gen/
# tauri icon also emits mobile platform icons; this app targets Windows desktop
# only (mobile not set up — src-tauri/gen is ignored), so they're byproducts.
src-tauri/icons/android/
src-tauri/icons/ios/
