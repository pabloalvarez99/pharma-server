target/
**/*.rs.bk
Cargo.lock.bak
*.pdb
.env
.env.local
# Config local del operador/dev (regla #8: JWT secret, CORS de dev, rutas de DB).
# `config/default.toml` SÍ se commitea; local.toml NUNCA.
config/local.toml
data/
*.db
*.log
installer/wix/target/
installer/wix/*.wixobj
installer/wix/*.wixpdb
installer/wix/*.msi
.idea/
.vscode/
*.swp
.DS_Store
.claude/scheduled_tasks.lock

# DB-coordination dumps (real catalog data, may contain pricing — never commit)
demo-data/

# Custom cargo target dir used across sessions (CARGO_TARGET_DIR=target-shared)
target-shared/

# Notas locales del fundador (historial de prompts/sesiones), nunca commitear
a*_historial_*.md
a_historial_*.md
claudeapp.md

# Code-signing private key (ADR-0008). pilot.cer (public) IS committed; pilot.pfx NEVER.
installer/sign/*.pfx
installer/sign/.cert-password.txt

# Local CDN staging from scripts/publish-updater-artifacts.ps1 (never upload secrets).
dist-updater/

# Sandbox smoke artifacts (logs, install.log, verdicts) — transient, never committed.
installer/sandbox/out/

# Solo esta maquina: notas, scratch, rescates y credenciales. NUNCA a git.
# Estaba documentado en el README como "gitignored" pero la entrada no existia:
# un `git add .` se llevaba local/credenciales-rescatadas/ al historial.
local/
